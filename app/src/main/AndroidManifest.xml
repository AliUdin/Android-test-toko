<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ali.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".aktivity.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".aktivity.MainActivity" />
        <activity android:name=".aktivity.Pembeli" />
        <activity android:name=".aktivity.Pelapak" />
        <activity android:name=".Daftar" />
        <activity android:name=".ItemProduk" />
        <activity android:name=".ItemToko" />
        <activity android:name=".Toko" />
        <activity android:name=".TambahProduk"></activity>
    </application>

</manifest>